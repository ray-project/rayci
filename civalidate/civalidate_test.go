package civalidate

import "testing"

func TestNothing(t *testing.T) {
	// This test exists to include the package in coverage measurement
	// but doesn't exercise any code, resulting in 0% coverage.
}
